spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.center}
        namespace: ${nacos.namespace}
      config:
        server-addr: ${nacos.center}
        namespace: ${nacos.namespace}
        file-extension: yaml